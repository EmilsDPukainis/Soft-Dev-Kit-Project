# Soft-Dev-Kit-Project

for installing packages: 
  icacls install.sh /grant Everyone:RX 
  Then to install them use   ./install.sh

for starting the page and server: 
  npm start (This will use the start-app)
